{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "dev": "concurrently --names \"libs,wireframe,api,sandbox\" --prefix-colors \"blue,green,yellow,magenta\" \"pnpm run dev:libs\" \"pnpm --filter @workspace/wireframe-library run dev\" \"pnpm --filter @workspace/api-server run dev\" \"pnpm --filter @workspace/mockup-sandbox run dev\"",
    "dev:libs": "tsc --build --watch",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "dependencies": {
    "@replit/connectors-sdk": "^0.4.1"
  },
  "devDependencies": {
    "concurrently": "^10.0.4",
    "prettier": "^3.9.5",
    "typescript": "~5.9.3"
  }
}
