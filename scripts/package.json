{
  "name": "@workspace/scripts",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "hello": "tsx ./src/hello.ts",
    "validate:manifest": "tsx ./src/validate-manifest.ts",
    "generate:tokens": "tsx ./src/generate-tokens.ts",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "ajv": "^8.20.0",
    "tsx": "catalog:"
  }
}
